#ifndef _FILESEARCH_ENGINE_
#define _FILESEARCH_ENGINE_

#include <string>
#include <vector>

using namespace std;

enum t_scan_result {scan_ok = 0, scan_protocol_unknown};

class c_engine
{
private:
protected:
public:
	c_engine ();
	virtual ~c_engine ();
	//
	
};

#endif
